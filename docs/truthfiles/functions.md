# Functions

_Auto-generated by `scripts/gen_function_truthfile.py` — do not hand-edit. Regenerate after any docstring or function signature change:_
`.venv/bin/python scripts/gen_function_truthfile.py`

## src/toymc_cosmic/__init__.py

(no module-level functions or classes)

## src/toymc_cosmic/angular.py

- `AngularModel` (L12): Abstract base class for pluggable angular models.
- `AngularModel.sample_theta` (L16): Sample zenith angles in radians over the downward hemisphere; returns ndarray.
- `Cos2AngularModel` (L24): Default angular model with `g(theta) = cos^2(theta)`.
- `Cos2AngularModel.sample_theta` (L27): Sample `theta` from the downward-hemisphere `cos^2(theta)` sky law; returns ndarray.
- `TabulatedAngularModel` (L43): Generic angular model built from a tabulated weight function.
- `TabulatedAngularModel.__init__` (L51): Store the weight function and the integration grid size.
- `TabulatedAngularModel.sample_theta` (L58): Sample `theta` by numerically inverting a tabulated hemisphere CDF; returns ndarray.
- `_validate_count` (L91): Validate a positive sample count; returns None.

## src/toymc_cosmic/cli.py

- `build_parser` (L17): Build the CLI argument parser.
- `main` (L40): Run the CLI either in headless or GUI mode; returns the process exit code.
- `_validate_gui_arguments` (L64): Reject incompatible GUI argument combinations.
- `_print_headless_summary` (L73): Print the standard terminal summary for one simulation result.
- `_format_detector_rate_table` (L123): Return a detector rate table with aligned geometric and fired columns.
- `_format_rate_hz` (L153): Format a rate estimate with a configurable number of decimal digits.
- `_format_probability` (L159): Format a conditional probability with per-thousand precision.
- `_format_source_header` (L170): Format the source-specific header summary; returns text.
- `_render_progress` (L185): Render a single-line progress indicator for the CLI.

## src/toymc_cosmic/config.py

- `ConfigError` (L15): Raised when the YAML configuration is missing or contains invalid data.
- `SourceModelConfig` (L19): Marker base for the discriminated `source_model` config union.
- `CosmicSourceConfig` (L24): Cosmic-ray source settings for a downward sky intensity model.
- `BeamSourceConfig` (L38): Directed beam source settings; travels in `+z` (upstream to downstream).
- `ObjectSourceConfig` (L57): Mounted disk source settings for one-sided hemisphere emission.
- `ObjectSourceConfig.front_emission_rate_hz` (L78): Return the front-side particle emission rate; returns float.
- `MonteCarloConfig` (L88): Monte Carlo controls stored in the YAML file.
- `ConditionalConfig` (L95): Configuration for a conditional probability request.
- `OutputConfig` (L104): CLI formatting settings loaded from YAML.
- `Config` (L112): Validated engine configuration.
- `load_config` (L137): Load, validate, and normalize a YAML configuration file; returns the resulting Config.
- `_read_optional_seed` (L173): Read the optional random seed from the YAML file.
- `_parse_source_model` (L186): Parse and validate the `source_model` block; returns the matching config dataclass.
- `_parse_cosmic_source` (L201): Parse a `source_model.type: cosmic` block.
- `_parse_beam_source` (L212): Parse a `source_model.type: beam` block.
- `_parse_object_source` (L227): Parse a `source_model.type: object` block.
- `_read_float_tuple` (L280): Read a fixed-length list of numeric values as a tuple of floats.
- `_parse_monte_carlo` (L293): Parse Monte Carlo settings.
- `_parse_detectors` (L304): Parse and validate detector definitions.
- `_parse_logic` (L337): Parse logic expressions and validate detector references; returns the validated expressions and conditional configs.
- `_validate_expression_names` (L389): Validate that a logic expression references known detectors only.
- `_parse_gui` (L402): Preserve optional GUI configuration without engine-level interpretation.
- `_parse_output` (L411): Parse optional CLI output-format settings.
- `_read_float` (L432): Read a numeric field and convert it to float.
- `_read_positive_float` (L439): Read a strictly positive numeric field.
- `_read_non_negative_int` (L447): Read a non-negative integer setting from the YAML file.

## src/toymc_cosmic/geometry.py

- `Detector` (L11): Axis-aligned detector volume used by the engine.
- `Detector.__post_init__` (L26): Normalize array-like inputs and validate basic detector fields.
- `Detector.lower_bounds` (L47): Return the detector lower bounds `(xmin, ymin, zmin)`.
- `Detector.upper_bounds` (L52): Return the detector upper bounds `(xmax, ymax, zmax)`.
- `bounding_box` (L57): Return the global `(xmin, xmax, ymin, ymax)` detector bounding box.
- `bounding_box_3d` (L71): Return the global `(xmin, xmax, ymin, ymax, zmin, zmax)` detector bounding box.
- `enclosing_sphere` (L87): Return the detector-stack enclosing sphere center and padded radius.
- `reference_z` (L106): Return the highest detector top face used as the track origin plane.
- `min_reference_z` (L112): Return the lowest detector bottom face used as the beam upstream origin plane.
- `intersect` (L118): Return a boolean crossing matrix with shape `(n_events, n_detectors)`.
- `_require_detectors` (L184): Raise if an operation that needs detectors is called with none.
- `_validate_track_arrays` (L190): Validate the track arrays used by the intersection routine.

## src/toymc_cosmic/logic.py

- `evaluate` (L10): Evaluate a boolean detector expression against array-valued inputs; returns a per-event boolean array.
- `extract_names` (L25): Return the detector names referenced by a validated expression.
- `_parse_expression` (L37): Parse and validate a boolean detector expression; returns the parsed AST expression.
- `_validate_node` (L51): Recursively validate that only simple boolean syntax is used.
- `_evaluate_node` (L74): Recursively evaluate a validated AST node; returns the resulting boolean array.

## src/toymc_cosmic/rates.py

- `RateEstimate` (L18): Rate estimate with a simple binomial statistical uncertainty.
- `ProbabilityEstimate` (L28): Conditional probability estimate with binomial uncertainty.
- `binomial_rate` (L37): Convert a passing-event count into a rate estimate.
- `binomial_probability` (L50): Convert joint and conditional counts into a probability estimate.
- `detector_rates` (L68): Return geometric and fired rates for every detector.
- `logic_rates` (L93): Return geometric and fired rates for a detector logic expression.
- `conditional_probability` (L115): Return `P(numerator | given)` evaluated in one detector-response mode.

## src/toymc_cosmic/response.py

- `apply_response` (L8): Apply independent Bernoulli firing to every geometric crossing; returns the resulting fired boolean matrix.

## src/toymc_cosmic/simulation.py

- `SimulationResult` (L23): Container for the data produced by one Monte Carlo run.
- `run_simulation` (L45): Run the full headless Monte Carlo pipeline; returns the completed SimulationResult.

## src/toymc_cosmic/source.py

- `SourceModel` (L21): Abstract base class for pluggable source models.
- `SourceModel.generate` (L25): Generate `count` source tracks; returns Tracks.
- `SourceModel.total_rate_hz` (L29): Return the total physical event rate represented by the sampling, in Hz.
- `SourceModel.spatial_bounds` (L33): Return the source footprint bounds `(xmin,xmax,ymin,ymax,zmin,zmax)`.
- `CosmicSourceModel` (L39): Downward cosmic-ray source sampled from a padded enclosing sphere.
- `CosmicSourceModel.__init__` (L42): Store the cosmic source parameters and build the sky angular model.
- `CosmicSourceModel.generate` (L53): Sample tracks from an enclosing sphere; returns Tracks.
- `CosmicSourceModel.total_rate_hz` (L77): Return the auxiliary sphere-entry rate implied by the configured plane flux.
- `CosmicSourceModel.spatial_bounds` (L82): Return the enclosing sphere's axis-aligned bounding box.
- `CosmicSourceModel._enclosing_sphere` (L96): Compute and cache the padded detector enclosing sphere; returns `(center, radius)`.
- `BeamSourceModel` (L106): Directed beam source traveling in `+z` (upstream to downstream).
- `BeamSourceModel.__init__` (L109): Store the beam parameters.
- `BeamSourceModel.generate` (L126): Sample tracks from the upstream face traveling in `+z`; returns Tracks.
- `BeamSourceModel._uniform_radius` (L143): Return the `uniform`-profile disk radius; returns float.
- `BeamSourceModel._gaussian_sigma` (L147): Return the `gaussian`-profile `(sigma_x, sigma_y)` derived from the FWHM.
- `BeamSourceModel._sample_transverse_positions` (L151): Sample transverse `(x, y)` positions matching the configured profile.
- `BeamSourceModel._sample_gaussian_transverse_positions` (L164): Sample a truncated 2D gaussian beam spot; returns `(x_positions, y_positions)`.
- `BeamSourceModel.total_rate_hz` (L191): Return the source's total physical rate, in Hz.
- `BeamSourceModel._footprint_area` (L197): Return the beam footprint area used by the total-rate normalization; returns float.
- `BeamSourceModel.spatial_bounds` (L203): Return the beam footprint bounds from the upstream face to the far detector face.
- `ObjectSourceModel` (L220): Mounted one-sided disk source emitting into a forward hemisphere.
- `ObjectSourceModel.__init__` (L223): Store the disk-source parameters and normalize its emitting normal.
- `ObjectSourceModel.generate` (L244): Sample emission points on the disk and forward directions; returns Tracks.
- `ObjectSourceModel._sample_positions` (L253): Sample emission points uniformly on the disk surface; returns ndarray.
- `ObjectSourceModel._sample_directions` (L264): Sample forward-hemisphere directions for the configured angular model; returns ndarray.
- `ObjectSourceModel.total_rate_hz` (L279): Return the configured front-side particle emission rate, in Hz.
- `ObjectSourceModel.spatial_bounds` (L283): Return the oriented emitting disk's axis-aligned bounds.
- `_sample_disk_offsets_perpendicular_to_vectors` (L299): Sample uniform disk offsets perpendicular to unit directions; returns ndarray.
- `_orthonormal_basis_from_unit_vectors` (L314): Build orthonormal bases perpendicular to unit vectors; returns `(basis_u, basis_v)`.
- `_normalize_vector` (L326): Normalize a non-zero vector; returns the normalized vector.
- `build_source_model` (L334): Build the concrete SourceModel matching a validated source config.

## src/toymc_cosmic/tracks.py

- `Tracks` (L11): Generated straight-line tracks for the Monte Carlo.
- `Tracks.__post_init__` (L22): Validate the track container arrays.

## src/toymc_cosmic/gui/__init__.py

(no module-level functions or classes)

## src/toymc_cosmic/gui/buttons.py

- `NavigationButtonSpec` (L36): One icon-only action button shown in the event display.
- `build_navigation_button_specs` (L44): Return the fixed horizontal button row, left-aligned directly under the axes triad.
- `build_button_texture_pixels` (L57): Build one RGB texture for a button with a centered icon.
- `_draw_button_icon` (L84): Paint one of the supported button icons into the texture array.
- `create_button_image_data` (L131): Convert a texture RGB array into the image object expected by VTK buttons.
- `create_textured_button_widget` (L139): Create one low-level textured VTK button widget.

## src/toymc_cosmic/gui/config.py

- `GUIConfig` (L20): Normalized visualization settings for the optional GUI.
- `GUIConfig.detector_color` (L34): Return the configured base color for one detector.
- `load_gui_config` (L39): Normalize the optional `gui:` mapping from the engine config.
- `_read_color` (L90): Read a GUI color value as a named string or RGB triple.
- `_read_float_value` (L108): Read a numeric field and convert it to float.
- `_read_positive_float` (L115): Read a strictly positive float from the GUI settings.
- `_read_unit_interval_float` (L123): Read a float in the inclusive `[0, 1]` range from the GUI settings.

## src/toymc_cosmic/gui/event_state.py

- `PreparedConditional` (L25): Precomputed per-event booleans for one configured conditional.
- `EventDisplayState` (L38): One viewer state for one event and one relevant conditional.
- `EventNavigator` (L64): Stateful navigator for nested event/conditional stepping in the event display.
- `EventNavigator.__init__` (L67): Build a lazy navigator over event display states.
- `EventNavigator.current_state` (L84): Return the state currently selected by the navigator.
- `EventNavigator.next_state` (L89): Advance to the next conditional state or the next event; returns the resulting state.
- `EventNavigator.previous_state` (L101): Move back to the previous conditional state or previous event; returns the resulting state.
- `EventNavigator.state_position` (L113): Return the current event/state position for tests and controller checks.
- `EventNavigator._states_for_current_event` (L117): Return the cached states for the currently selected relevant event.
- `prepare_conditionals` (L131): Precompute per-event boolean arrays for every configured conditional.
- `collect_relevant_event_indices` (L152): Return event indices relevant to at least one conditional in geometric mode.
- `build_event_states_for_event` (L163): Return all viewer states for one event in the configured stepping order.

## src/toymc_cosmic/gui/layout.py

(no module-level functions or classes)

## src/toymc_cosmic/gui/legend.py

- `build_legend_lines` (L20): Build the fixed eight-line status/legend content shown in the boxed overlay.
- `build_legend_sentence_text` (L43): Build the single dynamic 'Given .../ Numerator ...' sentence line.

## src/toymc_cosmic/gui/scene.py

- `show_geometry_only` (L25): Open a static detector-only 3D scene without running the Monte Carlo.
- `startup_view_up` (L38): Return the startup camera's up vector for a source type.
- `build_plotter` (L51): Create a PyVista plotter populated with detector meshes.
- `render_source_shape` (L98): Render the object-source disk once for `object`-type sources; no-op otherwise.
- `compute_axes_viewport` (L122): Return the (xstart, ystart, xend, yend) axes viewport, shifted from PyVista's default.
- `plotter_window_height_px` (L168): Return the plotter's current window height in pixels, or a safe fallback.
- `plotter_window_width_px` (L188): Return the plotter's current window width in pixels, or a safe fallback.
- `render_detector_colors` (L204): Render or update detector boxes using either base or event colors.
- `detector_scene_bounds` (L238): Return the full axis-aligned bounds of the detector setup.
- `build_startup_camera_position` (L255): Build a deterministic startup camera with direction parallel to `(1, 1, 1)`.
- `plotter_viewport_aspect_ratio` (L316): Return the current plotter width/height ratio or a deterministic fallback.
- `_scene_bounds_corners` (L330): Return the eight corners of an axis-aligned bounds box.
- `apply_camera_position` (L350): Apply a fixed camera tuple without letting PyVista choose a new direction.
- `_require_pyvista` (L363): Import PyVista only when the GUI is actually requested.

## src/toymc_cosmic/gui/track_bounds.py

- `DisplayBounds` (L23): Axis-aligned box used to clip the displayed event track.
- `compute_display_bounds` (L34): Compute the finite box used to clip displayed event tracks.
- `_detector_bounds` (L49): Return the detector stack's own axis-aligned bounds.
- `_union_bounds` (L55): Return the axis-aligned union of two `(xmin,xmax,ymin,ymax,zmin,zmax)` boxes.
- `_pad_bounds` (L70): Pad every axis of a raw bounds tuple into a `DisplayBounds`.
- `_pad` (L86): Return `(axis_min, axis_max)` each padded outward by 10% of the span or magnitude.
- `clip_line_to_bounds` (L94): Clip an infinite line to the finite display box.

## src/toymc_cosmic/gui/viewer.py

- `show_event_display` (L60): Open the step-by-step event display viewer.
- `EventDisplayController` (L87): PyVista scene controller that redraws as the user steps through events.
- `EventDisplayController.__init__` (L90): Build the controller and its base detector scene.
- `EventDisplayController.show` (L118): Register callbacks, draw the first state, and open the window.
- `EventDisplayController._show_next_state` (L130): Advance to the next state and redraw the scene.
- `EventDisplayController._show_previous_state` (L135): Move back to the previous state and redraw the scene.
- `EventDisplayController._close` (L140): Close the PyVista window.
- `EventDisplayController._reset_view` (L144): Restore the initial camera without changing event navigation state.
- `EventDisplayController._render_current_state` (L149): Redraw detectors, track, and status text for the current state.
- `EventDisplayController._render_legend` (L194): Draw the merged status/legend block as one framed box plus one colored sentence.
- `EventDisplayController._build_detector_opacity_map` (L272): Dim detectors not referenced by the active conditional; returns the per-detector opacity map.
- `EventDisplayController._add_navigation_buttons` (L282): Add one horizontal row of icon-only action buttons under the axes widget.
- `EventDisplayController._add_action_button` (L294): Create one textured action button and keep its widget alive.
- `EventDisplayController._add_nav_help_text` (L329): Draw the static key-binding hint text once, at the bottom of the axes/button stack.
- `_require_pyvista` (L344): Import PyVista only when the GUI is actually requested.
