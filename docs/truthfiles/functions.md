# Functions

_Auto-generated by `scripts/gen_function_truthfile.py` — do not hand-edit. Regenerate after any docstring or function signature change:_
`.venv/bin/python scripts/gen_function_truthfile.py`

## src/toymc_cosmic/__init__.py

(no module-level functions or classes)

## src/toymc_cosmic/angular.py

- `AngularModel` (L12): Abstract base class for pluggable angular models.
- `AngularModel.sample_theta` (L16): Sample zenith angles in radians over the interval `[0, theta_max]`.
- `Cos2AngularModel` (L25): Default angular model with `g(theta) = cos^2(theta)`.
- `Cos2AngularModel.sample_theta` (L28): Sample `theta` using the closed-form inverse CDF for the cos^2 model.
- `TabulatedAngularModel` (L49): Generic angular model built from a tabulated weight function.
- `TabulatedAngularModel.__init__` (L56): Store the weight function and the integration grid size.
- `TabulatedAngularModel.sample_theta` (L63): Sample `theta` by numerically inverting a tabulated CDF.
- `_validate_theta_inputs` (L97): Validate shared angular sampling inputs.

## src/toymc_cosmic/cli.py

- `build_parser` (L17): Build the CLI argument parser.
- `main` (L40): Run the CLI either in headless or GUI mode.
- `_validate_gui_arguments` (L64): Reject incompatible GUI argument combinations.
- `_print_headless_summary` (L73): Print the standard terminal summary for one simulation result.
- `_format_detector_rate_table` (L126): Return a detector rate table with aligned geometric and fired columns.
- `_format_rate_hz` (L156): Format a rate estimate with a configurable number of decimal digits.
- `_format_probability` (L162): Format a conditional probability with per-thousand precision.
- `_render_progress` (L173): Render a single-line progress indicator for the CLI.

## src/toymc_cosmic/config.py

- `ConfigError` (L16): Raised when the YAML configuration is missing or contains invalid data.
- `AngularModelConfig` (L21): Angular model settings stored in the YAML file.
- `MonteCarloConfig` (L28): Monte Carlo controls stored in the YAML file.
- `ConditionalConfig` (L35): Configuration for a conditional probability request.
- `OutputConfig` (L44): CLI formatting settings loaded from YAML.
- `Config` (L52): Validated engine configuration.
- `load_config` (L80): Load, validate, and normalize a YAML configuration file.
- `_read_optional_seed` (L126): Read the optional random seed from the YAML file.
- `_parse_angular_model` (L135): Parse angular model settings.
- `_parse_monte_carlo` (L146): Parse Monte Carlo settings.
- `_parse_detectors` (L157): Parse and validate detector definitions.
- `_parse_logic` (L190): Parse logic expressions and validate detector references.
- `_validate_expression_names` (L242): Validate that a logic expression references known detectors only.
- `_parse_gui` (L255): Preserve optional GUI configuration without engine-level interpretation.
- `_parse_output` (L264): Parse optional CLI output-format settings.
- `_read_float` (L285): Read a numeric field and convert it to float.
- `_read_non_negative_int` (L292): Read a non-negative integer setting from the YAML file.

## src/toymc_cosmic/geometry.py

- `Detector` (L12): Axis-aligned detector volume used by the engine.
- `Detector.__post_init__` (L27): Normalize array-like inputs and validate basic detector fields.
- `Detector.lower_bounds` (L48): Return the detector lower bounds `(xmin, ymin, zmin)`.
- `Detector.upper_bounds` (L53): Return the detector upper bounds `(xmax, ymax, zmax)`.
- `bounding_box` (L58): Return the global `(xmin, xmax, ymin, ymax)` detector bounding box.
- `vertical_extent` (L72): Return the total vertical extent of the detector setup.
- `generation_region` (L81): Return the unbiased generation rectangle and its area.
- `reference_z` (L105): Return the highest detector top face used as the track origin plane.
- `intersect` (L111): Return a boolean crossing matrix with shape `(n_events, n_detectors)`.
- `_require_detectors` (L177): Raise if an operation that needs detectors is called with none.
- `_validate_track_arrays` (L183): Validate the track arrays used by the intersection routine.

## src/toymc_cosmic/logic.py

- `evaluate` (L10): Evaluate a boolean detector expression against array-valued inputs.
- `extract_names` (L25): Return the detector names referenced by a validated expression.
- `_parse_expression` (L37): Parse and validate a boolean detector expression.
- `_validate_node` (L51): Recursively validate that only simple boolean syntax is used.
- `_evaluate_node` (L74): Recursively evaluate a validated AST node.

## src/toymc_cosmic/rates.py

- `RateEstimate` (L18): Rate estimate with a simple binomial statistical uncertainty.
- `ProbabilityEstimate` (L28): Conditional probability estimate with binomial uncertainty.
- `binomial_rate` (L37): Convert a passing-event count into a rate estimate.
- `binomial_probability` (L51): Convert joint and conditional counts into a probability estimate.
- `detector_rates` (L69): Return geometric and fired rates for every detector.
- `logic_rates` (L96): Return geometric and fired rates for a detector logic expression.
- `conditional_probability` (L120): Return `P(numerator | given)` evaluated in one detector-response mode.

## src/toymc_cosmic/response.py

- `apply_response` (L8): Apply independent Bernoulli firing to every geometric crossing.

## src/toymc_cosmic/simulation.py

- `SimulationResult` (L23): Container for the data produced by one Monte Carlo run.
- `run_simulation` (L49): Run the full headless Monte Carlo pipeline.
- `_build_angular_model` (L134): Build the configured angular model.

## src/toymc_cosmic/tracks.py

- `Tracks` (L15): Generated straight-line tracks for the Monte Carlo.
- `Tracks.__post_init__` (L26): Validate the track container arrays.
- `generate_tracks` (L36): Generate straight downward tracks for the Monte Carlo.

## src/toymc_cosmic/gui/__init__.py

(no module-level functions or classes)

## src/toymc_cosmic/gui/buttons.py

- `NavigationButtonSpec` (L36): Describe one icon-only action button shown in the event display.
- `build_navigation_button_specs` (L44): Return the fixed horizontal button row, left-aligned directly under the axes triad.
- `build_button_texture_pixels` (L57): Build one RGB texture for a button with a centered icon.
- `_draw_button_icon` (L84): Paint one of the supported button icons into the texture array.
- `create_button_image_data` (L131): Convert a texture RGB array into the image object expected by VTK buttons.
- `create_textured_button_widget` (L139): Create one low-level textured VTK button widget.

## src/toymc_cosmic/gui/config.py

- `GUIConfig` (L20): Normalized visualization settings for the optional GUI.
- `GUIConfig.detector_color` (L32): Return the configured base color for one detector.
- `load_gui_config` (L37): Normalize the optional `gui:` mapping from the engine config.
- `_read_color` (L86): Read a GUI color value as a named string or RGB triple.
- `_read_positive_float` (L104): Read a strictly positive float from the GUI settings.

## src/toymc_cosmic/gui/event_state.py

- `PreparedConditional` (L25): Precomputed per-event booleans for one configured conditional.
- `EventDisplayState` (L38): One viewer state for one event and one relevant conditional.
- `EventNavigator` (L64): Manage nested event/conditional stepping for the event display.
- `EventNavigator.__init__` (L67): Build a lazy navigator over event display states.
- `EventNavigator.current_state` (L84): Return the state currently selected by the navigator.
- `EventNavigator.next_state` (L89): Advance to the next conditional state or the next event.
- `EventNavigator.previous_state` (L101): Move back to the previous conditional state or previous event.
- `EventNavigator.state_position` (L113): Return the current event/state position for tests and controller checks.
- `EventNavigator._states_for_current_event` (L117): Return the cached states for the currently selected relevant event.
- `prepare_conditionals` (L131): Precompute per-event boolean arrays for every configured conditional.
- `collect_relevant_event_indices` (L152): Return event indices relevant to at least one conditional in geometric mode.
- `build_event_states_for_event` (L163): Return all viewer states for one event in the configured stepping order.

## src/toymc_cosmic/gui/layout.py

(no module-level functions or classes)

## src/toymc_cosmic/gui/legend.py

- `build_legend_lines` (L20): Build the fixed eight-line status/legend content shown in the boxed overlay.
- `build_legend_sentence_text` (L43): Build the single dynamic 'Given .../ Numerator ...' sentence line.

## src/toymc_cosmic/gui/scene.py

- `show_geometry_only` (L24): Open a static detector-only 3D scene without running the Monte Carlo.
- `build_plotter` (L36): Create a PyVista plotter populated with detector meshes.
- `compute_axes_viewport` (L81): Return the (xstart, ystart, xend, yend) axes viewport, shifted from PyVista's default.
- `plotter_window_height_px` (L127): Return the plotter's current window height in pixels, or a safe fallback.
- `plotter_window_width_px` (L147): Return the plotter's current window width in pixels, or a safe fallback.
- `render_detector_colors` (L163): Render or update detector boxes using either base or event colors.
- `detector_scene_bounds` (L197): Return the full axis-aligned bounds of the detector setup.
- `build_startup_camera_position` (L214): Build a deterministic startup camera with direction parallel to `(1, 1, 1)`.
- `plotter_viewport_aspect_ratio` (L274): Return the current plotter width/height ratio or a deterministic fallback.
- `_scene_bounds_corners` (L288): Return the eight corners of an axis-aligned bounds box.
- `apply_camera_position` (L308): Apply a fixed camera tuple without letting PyVista choose a new direction.
- `_require_pyvista` (L321): Import PyVista only when the GUI is actually requested.

## src/toymc_cosmic/gui/track_bounds.py

- `DisplayBounds` (L22): Axis-aligned box used to clip the displayed event track.
- `compute_display_bounds` (L33): Compute the finite box used to clip displayed event tracks.
- `clip_line_to_bounds` (L55): Clip an infinite line to the finite display box.

## src/toymc_cosmic/gui/viewer.py

- `show_event_display` (L58): Open the step-by-step event display viewer.
- `EventDisplayController` (L84): Own the PyVista scene and update it as the user steps through events.
- `EventDisplayController.__init__` (L87): Build the controller and its base detector scene.
- `EventDisplayController.show` (L113): Register callbacks, draw the first state, and open the window.
- `EventDisplayController._show_next_state` (L125): Advance to the next state and redraw the scene.
- `EventDisplayController._show_previous_state` (L130): Move back to the previous state and redraw the scene.
- `EventDisplayController._close` (L135): Close the PyVista window.
- `EventDisplayController._reset_view` (L139): Restore the initial camera without changing event navigation state.
- `EventDisplayController._render_current_state` (L144): Redraw detectors, track, and status text for the current state.
- `EventDisplayController._render_legend` (L189): Draw the merged status/legend block as one framed box plus one colored sentence.
- `EventDisplayController._build_detector_opacity_map` (L267): Dim detectors not referenced by the active conditional.
- `EventDisplayController._add_navigation_buttons` (L277): Add one horizontal row of icon-only action buttons under the axes widget.
- `EventDisplayController._add_action_button` (L289): Create one textured action button and keep its widget alive.
- `EventDisplayController._add_nav_help_text` (L324): Draw the static key-binding hint text once, at the bottom of the axes/button stack.
- `_require_pyvista` (L339): Import PyVista only when the GUI is actually requested.
