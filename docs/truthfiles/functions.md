# Functions

_Auto-generated by `scripts/gen_function_truthfile.py` — do not hand-edit. Regenerate after any docstring or function signature change:_
`.venv/bin/python scripts/gen_function_truthfile.py`

## src/toymc_cosmic/__init__.py

(no module-level functions or classes)

## src/toymc_cosmic/angular.py

- `AngularModel` (L12): Abstract base class for pluggable angular models.
- `AngularModel.sample_theta` (L16): Sample zenith angles in radians over the interval `[0, theta_max]`.
- `Cos2AngularModel` (L25): Default angular model with `g(theta) = cos^2(theta)`.
- `Cos2AngularModel.sample_theta` (L28): Sample `theta` using the closed-form inverse CDF for the cos^2 model.
- `TabulatedAngularModel` (L49): Generic angular model built from a tabulated weight function.
- `TabulatedAngularModel.__init__` (L56): Store the weight function and the integration grid size.
- `TabulatedAngularModel.sample_theta` (L63): Sample `theta` by numerically inverting a tabulated CDF.
- `_validate_theta_inputs` (L97): Validate shared angular sampling inputs.

## src/toymc_cosmic/cli.py

- `build_parser` (L17): Build the CLI argument parser.
- `main` (L40): Run the CLI either in headless or GUI mode; returns the process exit code.
- `_validate_gui_arguments` (L64): Reject incompatible GUI argument combinations.
- `_print_headless_summary` (L73): Print the standard terminal summary for one simulation result.
- `_format_detector_rate_table` (L126): Return a detector rate table with aligned geometric and fired columns.
- `_format_rate_hz` (L156): Format a rate estimate with a configurable number of decimal digits.
- `_format_probability` (L162): Format a conditional probability with per-thousand precision.
- `_render_progress` (L173): Render a single-line progress indicator for the CLI.

## src/toymc_cosmic/config.py

- `ConfigError` (L16): Raised when the YAML configuration is missing or contains invalid data.
- `SourceModelConfig` (L20): Marker base for the discriminated `source_model` config union.
- `CosmicSourceConfig` (L25): Cosmic-ray source settings: zenith-angle sampling over a downward cone.
- `BeamSourceConfig` (L40): Directed beam source settings; travels in `+z` (upstream to downstream).
- `ObjectSourceConfig` (L60): Radioactive point/volume source settings; emits isotropically over 4*pi.
- `MonteCarloConfig` (L78): Monte Carlo controls stored in the YAML file.
- `ConditionalConfig` (L85): Configuration for a conditional probability request.
- `OutputConfig` (L94): CLI formatting settings loaded from YAML.
- `Config` (L102): Validated engine configuration.
- `load_config` (L127): Load, validate, and normalize a YAML configuration file; returns the resulting Config.
- `_read_optional_seed` (L163): Read the optional random seed from the YAML file.
- `_parse_source_model` (L177): Parse and validate the `source_model` block; returns the matching config dataclass.
- `_parse_cosmic_source` (L192): Parse a `source_model.type: cosmic` block.
- `_parse_beam_source` (L213): Parse a `source_model.type: beam` block.
- `_parse_object_source` (L233): Parse a `source_model.type: object` block.
- `_read_size_list` (L250): Read `source_model.size` as a non-empty tuple of floats.
- `_validate_size_length` (L257): Validate that `size` has the expected length and strictly positive components.
- `_read_float_tuple` (L266): Read a fixed-length list of numeric values as a tuple of floats.
- `_parse_monte_carlo` (L273): Parse Monte Carlo settings.
- `_parse_detectors` (L284): Parse and validate detector definitions.
- `_parse_logic` (L317): Parse logic expressions and validate detector references; returns the validated expressions and conditional configs.
- `_validate_expression_names` (L369): Validate that a logic expression references known detectors only.
- `_parse_gui` (L382): Preserve optional GUI configuration without engine-level interpretation.
- `_parse_output` (L391): Parse optional CLI output-format settings.
- `_read_float` (L412): Read a numeric field and convert it to float.
- `_read_non_negative_int` (L419): Read a non-negative integer setting from the YAML file.

## src/toymc_cosmic/geometry.py

- `Detector` (L12): Axis-aligned detector volume used by the engine.
- `Detector.__post_init__` (L27): Normalize array-like inputs and validate basic detector fields.
- `Detector.lower_bounds` (L48): Return the detector lower bounds `(xmin, ymin, zmin)`.
- `Detector.upper_bounds` (L53): Return the detector upper bounds `(xmax, ymax, zmax)`.
- `bounding_box` (L58): Return the global `(xmin, xmax, ymin, ymax)` detector bounding box.
- `vertical_extent` (L72): Return the total vertical extent of the detector setup.
- `generation_region` (L81): Return the unbiased generation rectangle and its area.
- `reference_z` (L105): Return the highest detector top face used as the track origin plane.
- `min_reference_z` (L111): Return the lowest detector bottom face used as the beam upstream origin plane.
- `intersect` (L117): Return a boolean crossing matrix with shape `(n_events, n_detectors)`.
- `_require_detectors` (L183): Raise if an operation that needs detectors is called with none.
- `_validate_track_arrays` (L189): Validate the track arrays used by the intersection routine.

## src/toymc_cosmic/logic.py

- `evaluate` (L10): Evaluate a boolean detector expression against array-valued inputs; returns a per-event boolean array.
- `extract_names` (L25): Return the detector names referenced by a validated expression.
- `_parse_expression` (L37): Parse and validate a boolean detector expression; returns the parsed AST expression.
- `_validate_node` (L51): Recursively validate that only simple boolean syntax is used.
- `_evaluate_node` (L74): Recursively evaluate a validated AST node; returns the resulting boolean array.

## src/toymc_cosmic/rates.py

- `RateEstimate` (L18): Rate estimate with a simple binomial statistical uncertainty.
- `ProbabilityEstimate` (L28): Conditional probability estimate with binomial uncertainty.
- `binomial_rate` (L37): Convert a passing-event count into a rate estimate.
- `binomial_probability` (L50): Convert joint and conditional counts into a probability estimate.
- `detector_rates` (L68): Return geometric and fired rates for every detector.
- `logic_rates` (L93): Return geometric and fired rates for a detector logic expression.
- `conditional_probability` (L115): Return `P(numerator | given)` evaluated in one detector-response mode.

## src/toymc_cosmic/response.py

- `apply_response` (L8): Apply independent Bernoulli firing to every geometric crossing; returns the resulting fired boolean matrix.

## src/toymc_cosmic/simulation.py

- `SimulationResult` (L23): Container for the data produced by one Monte Carlo run.
- `run_simulation` (L45): Run the full headless Monte Carlo pipeline; returns the completed SimulationResult.

## src/toymc_cosmic/source.py

- `SourceModel` (L20): Abstract base class for pluggable source models.
- `SourceModel.generate` (L24): Generate `count` (origin, direction) tracks for this source.
- `SourceModel.total_rate_hz` (L28): Return the total physical event rate this source's sampling represents, in Hz.
- `SourceModel.spatial_bounds` (L32): Return this source's own axis-aligned footprint `(xmin,xmax,ymin,ymax,zmin,zmax)`.
- `CosmicSourceModel` (L38): Downward cosmic-ray source: `cos^2` zenith sampling over a flat generation plane.
- `CosmicSourceModel.__init__` (L41): Store the cosmic source parameters and build its angular model.
- `CosmicSourceModel.generate` (L47): Sample tracks from a flat plane above the detector stack; returns Tracks.
- `CosmicSourceModel.total_rate_hz` (L71): Return `flux_hz_per_cm2 * generation area`.
- `CosmicSourceModel.spatial_bounds` (L76): Return the generation rectangle at the flat origin plane's height.
- `BeamSourceModel` (L85): Directed beam source traveling in `+z` (upstream to downstream).
- `BeamSourceModel.__init__` (L88): Store the beam parameters; raises NotImplementedError for the unimplemented divergence profile.
- `BeamSourceModel.generate` (L109): Sample tracks from the upstream face traveling in `+z`; returns Tracks.
- `BeamSourceModel._sample_transverse_positions` (L123): Sample `(x, y)` transverse positions matching the configured profile.
- `BeamSourceModel.total_rate_hz` (L139): Return `flux_hz_per_cm2 * transverse footprint area`.
- `BeamSourceModel._footprint_area` (L143): Return the disk (`uniform`) or FWHM-ellipse (`gaussian`) footprint area.
- `BeamSourceModel.spatial_bounds` (L150): Return the beam's transverse footprint spanning from the upstream face to the far detector face.
- `ObjectSourceModel` (L166): Radioactive point/volume source emitting isotropically over the full 4*pi sphere.
- `ObjectSourceModel.__init__` (L169): Store the object source parameters.
- `ObjectSourceModel.generate` (L185): Sample emission points inside the volume and isotropic directions; returns Tracks.
- `ObjectSourceModel._sample_positions` (L194): Sample emission points uniformly inside the configured volume.
- `ObjectSourceModel.total_rate_hz` (L221): Return the configured total activity, unchanged.
- `ObjectSourceModel.spatial_bounds` (L225): Return the source volume's own axis-aligned bounding box.
- `_sample_isotropic_directions` (L249): Sample unit direction vectors uniformly over the full 4*pi sphere.
- `build_source_model` (L261): Build the concrete `SourceModel` matching a validated source config.

## src/toymc_cosmic/tracks.py

- `Tracks` (L11): Generated straight-line tracks for the Monte Carlo.
- `Tracks.__post_init__` (L22): Validate the track container arrays.

## src/toymc_cosmic/gui/__init__.py

(no module-level functions or classes)

## src/toymc_cosmic/gui/buttons.py

- `NavigationButtonSpec` (L36): One icon-only action button shown in the event display.
- `build_navigation_button_specs` (L44): Return the fixed horizontal button row, left-aligned directly under the axes triad.
- `build_button_texture_pixels` (L57): Build one RGB texture for a button with a centered icon.
- `_draw_button_icon` (L84): Paint one of the supported button icons into the texture array.
- `create_button_image_data` (L131): Convert a texture RGB array into the image object expected by VTK buttons.
- `create_textured_button_widget` (L139): Create one low-level textured VTK button widget.

## src/toymc_cosmic/gui/config.py

- `GUIConfig` (L20): Normalized visualization settings for the optional GUI.
- `GUIConfig.detector_color` (L34): Return the configured base color for one detector.
- `load_gui_config` (L39): Normalize the optional `gui:` mapping from the engine config.
- `_read_color` (L90): Read a GUI color value as a named string or RGB triple.
- `_read_positive_float` (L108): Read a strictly positive float from the GUI settings.
- `_read_unit_interval_float` (L119): Read a float in the inclusive `[0, 1]` range from the GUI settings.

## src/toymc_cosmic/gui/event_state.py

- `PreparedConditional` (L25): Precomputed per-event booleans for one configured conditional.
- `EventDisplayState` (L38): One viewer state for one event and one relevant conditional.
- `EventNavigator` (L64): Stateful navigator for nested event/conditional stepping in the event display.
- `EventNavigator.__init__` (L67): Build a lazy navigator over event display states.
- `EventNavigator.current_state` (L84): Return the state currently selected by the navigator.
- `EventNavigator.next_state` (L89): Advance to the next conditional state or the next event; returns the resulting state.
- `EventNavigator.previous_state` (L101): Move back to the previous conditional state or previous event; returns the resulting state.
- `EventNavigator.state_position` (L113): Return the current event/state position for tests and controller checks.
- `EventNavigator._states_for_current_event` (L117): Return the cached states for the currently selected relevant event.
- `prepare_conditionals` (L131): Precompute per-event boolean arrays for every configured conditional.
- `collect_relevant_event_indices` (L152): Return event indices relevant to at least one conditional in geometric mode.
- `build_event_states_for_event` (L163): Return all viewer states for one event in the configured stepping order.

## src/toymc_cosmic/gui/layout.py

(no module-level functions or classes)

## src/toymc_cosmic/gui/legend.py

- `build_legend_lines` (L20): Build the fixed eight-line status/legend content shown in the boxed overlay.
- `build_legend_sentence_text` (L43): Build the single dynamic 'Given .../ Numerator ...' sentence line.

## src/toymc_cosmic/gui/scene.py

- `show_geometry_only` (L25): Open a static detector-only 3D scene without running the Monte Carlo.
- `startup_view_up` (L38): Return the startup camera's up vector for a source type.
- `build_plotter` (L51): Create a PyVista plotter populated with detector meshes.
- `render_source_shape` (L98): Render the source volume once for `object`-type sources; no-op otherwise.
- `compute_axes_viewport` (L132): Return the (xstart, ystart, xend, yend) axes viewport, shifted from PyVista's default.
- `plotter_window_height_px` (L178): Return the plotter's current window height in pixels, or a safe fallback.
- `plotter_window_width_px` (L198): Return the plotter's current window width in pixels, or a safe fallback.
- `render_detector_colors` (L214): Render or update detector boxes using either base or event colors.
- `detector_scene_bounds` (L248): Return the full axis-aligned bounds of the detector setup.
- `build_startup_camera_position` (L265): Build a deterministic startup camera with direction parallel to `(1, 1, 1)`.
- `plotter_viewport_aspect_ratio` (L326): Return the current plotter width/height ratio or a deterministic fallback.
- `_scene_bounds_corners` (L340): Return the eight corners of an axis-aligned bounds box.
- `apply_camera_position` (L360): Apply a fixed camera tuple without letting PyVista choose a new direction.
- `_require_pyvista` (L373): Import PyVista only when the GUI is actually requested.

## src/toymc_cosmic/gui/track_bounds.py

- `DisplayBounds` (L23): Axis-aligned box used to clip the displayed event track.
- `compute_display_bounds` (L34): Compute the finite box used to clip displayed event tracks.
- `_detector_bounds` (L49): Return the detector stack's own axis-aligned bounds.
- `_union_bounds` (L57): Return the axis-aligned union of two `(xmin,xmax,ymin,ymax,zmin,zmax)` boxes.
- `_pad_bounds` (L72): Pad every axis of a raw bounds tuple into a `DisplayBounds`.
- `_padded_min` (L85): Return `axis_min` padded outward by 10% of its span or magnitude.
- `_padded_max` (L92): Return `axis_max` padded outward by 10% of its span or magnitude.
- `clip_line_to_bounds` (L99): Clip an infinite line to the finite display box.

## src/toymc_cosmic/gui/viewer.py

- `show_event_display` (L60): Open the step-by-step event display viewer.
- `EventDisplayController` (L87): PyVista scene controller that redraws as the user steps through events.
- `EventDisplayController.__init__` (L90): Build the controller and its base detector scene.
- `EventDisplayController.show` (L118): Register callbacks, draw the first state, and open the window.
- `EventDisplayController._show_next_state` (L130): Advance to the next state and redraw the scene.
- `EventDisplayController._show_previous_state` (L135): Move back to the previous state and redraw the scene.
- `EventDisplayController._close` (L140): Close the PyVista window.
- `EventDisplayController._reset_view` (L144): Restore the initial camera without changing event navigation state.
- `EventDisplayController._render_current_state` (L149): Redraw detectors, track, and status text for the current state.
- `EventDisplayController._render_legend` (L194): Draw the merged status/legend block as one framed box plus one colored sentence.
- `EventDisplayController._build_detector_opacity_map` (L272): Dim detectors not referenced by the active conditional; returns the per-detector opacity map.
- `EventDisplayController._add_navigation_buttons` (L282): Add one horizontal row of icon-only action buttons under the axes widget.
- `EventDisplayController._add_action_button` (L294): Create one textured action button and keep its widget alive.
- `EventDisplayController._add_nav_help_text` (L329): Draw the static key-binding hint text once, at the bottom of the axes/button stack.
- `_require_pyvista` (L344): Import PyVista only when the GUI is actually requested.
